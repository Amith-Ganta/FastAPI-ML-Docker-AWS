# Docker Hub Configuration (used by the CI/CD publish step)
# Set these as GitHub repository secrets: DOCKER_USERNAME and DOCKER_TOKEN
DOCKER_USERNAME=tweakster24
DOCKER_TOKEN=dckr_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Published image (single source of truth)
IMAGE_NAME=tweakster24/insurance-premium-api
IMAGE_TAG=latest

# Optional: where the API is reachable once deployed (e.g. an EC2 host)
# Deployment is a plain `docker pull && docker run` of the image above.
API_HOST=204.236.207.23
API_PORT=8000

# GitHub Configuration (optional, for local tooling)
GITHUB_REPO_OWNER=Amith-Ganta
GITHUB_REPO_NAME=FastAPI-ML-Docker-AWS
