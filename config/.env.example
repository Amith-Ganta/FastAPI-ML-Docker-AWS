# Docker Hub (used by the CI/CD publish step)
# Set these as GitHub repository secrets: DOCKER_USERNAME and DOCKER_TOKEN
DOCKER_USERNAME=amith98480
DOCKER_TOKEN=dckr_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Published images (your own namespace; mirrored from the upstream working image)
IMAGE_PRIMARY=amith98480/insurance-premium-api
IMAGE_ALIAS=amith98480/fastapi-ml-docker-aws
IMAGE_TAG=latest
UPSTREAM_IMAGE=tweakster24/insurance-premium-api:latest

# AWS EC2 deploy target (set as GitHub secrets: AWS_HOST, AWS_SSH_KEY, AWS_USER)
AWS_HOST=204.236.207.23
AWS_USER=ubuntu
# AWS_SSH_KEY is the full PEM private-key contents (store as a GitHub secret, not here)

# GitHub Configuration (optional, for local tooling)
GITHUB_REPO_OWNER=Amith-Ganta
GITHUB_REPO_NAME=FastAPI-ML-Docker-AWS
